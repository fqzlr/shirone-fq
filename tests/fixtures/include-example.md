Included API marker lives on the first line for open-bounds tests.
This paragraph is outside every region and used by the line-range tests.

# region public-api
```ts
export function greet(name: string): string {
	return `Hello, ${name}!`;
}
```
# endregion
